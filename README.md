# CLA signatures

Machine-managed by `.github/workflows/cla.yml`. Signatures are recorded here
rather than through a hosted CLA service, so the record is public, auditable,
and does not depend on a third party keeping write access to this repository.

Do not edit by hand, and do not protect this branch: the CLA action needs to
commit to it when someone signs.
