---
---

Cross-repo compatibility gates (#23): CI workflow, API surface snapshot, `@typren/contract-tests`, CODEOWNERS, release canary. No published package's built output changes — the new devDependency and test files don't touch `dist`.
