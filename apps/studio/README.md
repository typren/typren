# @typren/studio

Stub. This will be the thin Next.js app that hosts `@typren/editor`: the App
Router wiring (`cms.config.ts`, the `/editor/[[...segment]]` catch-all route,
slice registry) that `typren init` (the `typren` package) scaffolds into a
consumer project today, made into a real, runnable reference app once
`@typren/editor` is complete. The editor's React components exist but are not
mounted anywhere yet.

No code yet.
