/** Conventional Commits enforcement — see @commitlint/config-conventional.
 *  Wired via the Husky `commit-msg` hook (.husky/commit-msg). */
export default { extends: ["@commitlint/config-conventional"] };
