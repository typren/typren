# registry

Placeholder. This will hold the slice registry — shareable, AI-authorable
slice components/schemas consumers can pull into their own `registry`/
`defaults`/`fieldSchema` (see `docs/slices.md` once it exists). Empty for now.
